# Synthetic mutation corpus, case 4 (spike/PHASE_D.md, D1): the case that
# should NOT fire. Only one variant -- there is no "vulnerable" version of
# a correctly-implemented deterministic nonce; the point is a negative
# result, not a before/after.
sink:
  name: rfc6979_nonce
  category: DETERMINISTIC_NONCE
  language: c
  file: nonce.c
  entry_symbol: rfc6979_nonce
expected:
  entropy_critical: false
  never_reported: true
