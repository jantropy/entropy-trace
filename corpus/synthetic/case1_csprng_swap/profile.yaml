# Synthetic mutation corpus, case 1: CSPRNG swapped for a seeded non-crypto
# PRNG at the terminal (spike/PHASE_D.md, D1).
sink:
  name: generate_key_material
  category: SEED_GENERATION
  language: c
  file: gen_key.c
  entry_symbol: generate_key_material
expected:
  vulnerable: NON_CRYPTO_PRNG
  patched: OS_CSPRNG
